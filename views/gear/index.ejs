<!DOCTYPE html>
<html lang="en">
  <%- include("../partials/head.ejs") %>

  <body>
    <%- include("../partials/header.ejs") %>
    <main>
      <div>
        <a href="/gear/new"><button>Create New Gear Listing</button></a>
        <a href="/user/logout"><button>Logout</button></a>

        <% for (gear of gear) { %>

        <article>
          <a href="/gear/<%= gear._id %>">
            <h2 class="h2yellow">
              <%= gear.name %>:  $<%= gear.price %>
            </h2>
          </a>
        </article>

        <% } %>
      </div>
    </main>
  </body>
</html>